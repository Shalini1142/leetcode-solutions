class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        l=len(set(nums))
        ls=len(nums)
        if ls>l:
            return True
        else:
            return False
